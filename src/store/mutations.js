export default {
  updateFilters(state, filters) {
    state.filters = filters
  },
  updateFonts(state, fonts) {
    state.fonts = fonts
  },
  changeView(state, view) {
    state.view = view
  },
}
